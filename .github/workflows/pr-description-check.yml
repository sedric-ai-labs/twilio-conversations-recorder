name: PR Description Check

on:
  pull_request:
    types: [opened, edited, synchronize]
    branches: [ master ]

permissions:
  issues: write
  pull-requests: write

jobs:
  check-pr-description:
    uses: sedric-ai-labs/shared-resources/.github/workflows/pr-description-check.yml@master
    secrets: inherit
